<h1>Edit Artist</h1>
<%= form_for @artist do |f| %>
  <label>Artist Name:</label>
  <%= f.text_field :name %>
  <label>Artist Bio:</label>
  <%= f.text_field :bio %>
  <%= f.submit "Update Artist" %>
<% end %>
