<h1>Artsist Edit Form</h1>

<%= form_for(@artist) do |f| %>
  <%= f.label :name %>:<br>
  <%= f.text_field :name %><br><br>
  <%= f.label :bio %>:<br>
  <%= f.text_area :bio %><br><br>

  <%= f.submit "Update Artist" %>
<% end %>