
<%= form_for(@artist) do |f| %>
<label for="artist[name]">Artist name:</label>
<%= f.text_field :name %>
<label for="artist[bio]">Artist bio:</label>
<%= f.text_field :bio %>
<%= f.submit %>
<% end %>
