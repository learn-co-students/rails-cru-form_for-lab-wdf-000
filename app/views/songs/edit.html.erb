<h3>Update Song</h3>
<%= form_for(@song) do |f| %>
		<label>Song Name:</label>
		<%= f.text_field :name %>
	
		<label>Artist:</label>
		<%= f.text_area :artist_id %>

		<label>Genre:</label>
		<%= f.text_area :genre_id %>
		
		<%= f.submit %>
	<% end %>